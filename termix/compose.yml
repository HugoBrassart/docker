services:
  termix:
    image: ghcr.io/lukegus/termix:latest
    container_name: termix
    restart: unless-stopped
#    ports:
#      - "8080:8080"
    volumes:
      - ./termix/termix-data:/app/data
    env_file:
      - .env
    environment:
      PORT: ${PORT}

      
   